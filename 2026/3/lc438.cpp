#include <iostream>
#include <vector>

using namespace std;

vector<int> findAnagrams(string s, string p) {
  vector<int> res;

  return res;
}

int main() {
  string s, p;
  for (int v : findAnagrams(s, p)) {
    cout << v;
  }
  return 0;
}